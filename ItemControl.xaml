<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiIosCollectionViewBugTest"
             x:Class="MauiIosCollectionViewBugTest.ItemControl"
             x:DataType="local:ItemViewModel">
    <Grid Margin="9,0,9,0">
        <!--Main body-->
        <Border Margin="9,12,9,12" Padding="0" StrokeThickness="3" BackgroundColor="Yellow">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="22" />
            </Border.StrokeShape>
            <Label Text="{Binding Name}" TextColor="Black" />
        </Border>
    </Grid>
</ContentView>